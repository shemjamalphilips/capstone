const bar = document.getElementById('bar');
const nav = document.getElementByI('navbar')

if (bar) {
    bar.addEventListener('click', () => {
    nav.classList.add('active')    
    })
}

{
    "editor.formatOnSave" ;true;
}